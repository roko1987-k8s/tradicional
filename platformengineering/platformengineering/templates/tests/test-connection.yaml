apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platformengineering.fullname" . }}-test-connection"
  labels:
    {{- include "platformengineering.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platformengineering.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
